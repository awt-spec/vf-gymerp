
El usuario quiere una explicación explícita del landing page de Elevate Lindora para poder recrearlo exactamente en otro contexto, **sin los accesos** (sin botones de login, sin links a `/gym/:slug/login`, sin auth).

Voy a leer el archivo `src/pages/Landing.tsx` completo para poder describir cada sección con precisión.
